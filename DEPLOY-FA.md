# 🚀 رساندن فروشگاه به یک دامنهٔ رایگان و دائمی

## چرا از داخل Arena نمی‌شود؟
محیط Arena یک سندباکس موقت است: بعد از چند دقیقه بی‌کاری می‌خوابد و همهٔ پردازش‌ها می‌میرند؛ پس هر لینکی که از داخلش ساخته شود (تونل یا پیش‌نمایش) عمرش به عمر بیداری محیط است. «دامنهٔ دائمی» یعنی یک سرور دائمی + یک آدرس ثابت — و سرویس‌های دائمی رایگان برای جلوگیری از سوءاستفاده، حداقل یک ثبت‌نام رایگان می‌خواهند. خبر خوب: **۹۵٪ کار را من کرده‌ام**؛ تو فقط چند کلیک ثبت‌نام می‌کنی.

---

## گزینهٔ الف — Render.com (کل سایت + پنل مدیر + API) ⭐ پیشنهادی
نتیجه: لینک دائمی مثل `https://greenapple-shop.onrender.com`

1. در **github.com** یک حساب رایگان بساز → دکمهٔ `+` → `New repository` → نام: `bander-shop` → Public → Create.
2. در صفحهٔ مخزن، `uploading an existing file` را بزن و **محتویات** این پروژه (پوشه‌های `server/`، `public/`، `tests/`، `tools/` و فایل‌های `package.json`، `render.yaml`، …) را آپلود و Commit کن. (خودِ zip را نه؛ محتویاتش را.)
3. در **render.com** با دکمهٔ «Sign in with GitHub» وارد شو → `New` → `Web Service` → مخزن `bander-shop` را انتخاب کن.
4. Render فایل `render.yaml` را خود پیدا می‌کند (پلن free، دستور start آماده) → `Create Web Service`.
5. یکی‌دو دقیقه صبر کن؛ لینک دائمی بالای صفحه ظاهر می‌شود. تمام. ✅

نکته‌های پلن رایگان Render:
- بعد از ~۱۵ دقیقه بی‌کاری می‌خوابد؛ کلیک بعدی در ~۳۰ ثانیه بیدارش می‌کند (لینک همان می‌ماند).
- فایل داده (`data/db.json`) روی دیسک موقت است؛ با هر استقرار مجدد، دادهٔ دمو از نو seed می‌شود. برای فروشگاه واقعی بعداً دیسک/دیتابیس اضافه می‌کنیم.
- ورود مدیر: `admin / Bander@1404` → **اولین کار: تغییر رمز از پنل مدیر.**

---

## گزینهٔ ب — Glitch.com (بدون گیت‌هاب، ساده‌ترین)
نتیجه: `https://bander-mobile.glitch.me`

1. در **glitch.com** با ایمیل ثبت‌نام کن → `New project` → `hello-express`.
2. همهٔ فایل‌های پیش‌فرض را پاک کن؛ بعد پوشه‌های `server/`، `public/` و فایل `package.json` را از داخل zip بکش و در صفحهٔ فایل‌ها رها کن (Glitch آپلود پوشه را پشتیبانی می‌کند).
3. Glitch خود `npm start` را اجرا می‌کند (اسکریپت start در package.json آماده است).
4. از Settings نام پروژه را بگذار `bander-mobile` → لینکت دائمی است. ✅

---

## گزینهٔ ج — فقط نسخهٔ آفلاین، روی هاست استاتیک (Netlify / GitHub Pages)
1. فایل `bander-mobile-offline.html` را به `index.html` تغییر نام بده.
2. در **app.netlify.com/drop** رهایش کن (بدون حساب: ۲۴ ساعت؛ با حساب رایگان: دائمی) یا روی GitHub Pages بگذار.
3. نکته: در این حالت داده‌ها در مرورگر هر بازدیدکننده ذخیره می‌شود (حالت دمو)؛ پنل مدیر مشترک و سبد مشترک ندارد. برای «ویترین + تست سریع» عالی است.

---

## اگر سرویسی IP ایران را محدود کرد
ثبت‌نام/ورود را با یک پروکسی ثابت غیرایرانی انجام بده (روال رایج برنامه‌نویس‌های ایرانی). **بعد از استقرار، خودِ سایت از ایران بدون پروکسی باز می‌شود** (دامنه‌های onrender.com / glitch.me / netlify.app فیلتر نیستند).

## دامنهٔ شخصی (.ir یا .com)
همهٔ سرویس‌های بالا «Custom Domain» رایگان دارند: یک دامنه بخر (سالانه هزینهٔ کمی دارد) و به همان سرویس وصل کن؛ از آن روز لینکت می‌شود مثلاً `bandermobile.ir`.

---

## 🤖 می‌خواهی خودم انجامش بدهم؟
یک حساب Render یا Glitch بساز و از تنظیمات حساب یک **API token** بگیر و برایم بفرست تا استقرار را از همین‌جا انجام دهم و لینک نهایی را تحویل بدهم.
⚠️ توکن = دسترسی کامل به آن حساب؛ فقط اگر به این گفتگو اعتماد داری این کار را بکن و بعد از استقرار توکن را باطل کن.
